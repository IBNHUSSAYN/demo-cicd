name: CI

  on:
    push:
      branches:
        - main

  jobs:
    run-script:
      runs-on: ubuntu-latest

      steps:
        - name: checkout respository
          uses: actions/checkout@v4

        - name: Run Bash Script
          run: |
            chmod +x app.sh
            ./app.sh

